; ── PHP sınırları (LiteSpeed lsphp .user.ini'yi bu dizin + alt dizinlerde okur) ──
; Kaynaklar PDF olarak geliyor; kitap PDF'leri büyük olabilir. Yükleme ve
; işleme sınırlarını PDF-çıkarım (api/pdf-extract.php) için yükseltiyoruz.
upload_max_filesize = 96M
post_max_size = 100M
memory_limit = 256M
max_execution_time = 0
max_input_time = -1
